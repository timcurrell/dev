# Raw strings
print "Raw strings are strings in which anything within the string is processed as-is."
print r"This allows you to type '\n' without it being interpreted as a newline character."
print "Raw strings are invoked by placing an 'R' or 'r' immediately ahead of the string."
